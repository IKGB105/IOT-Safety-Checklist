{
  "name": "checklist-shelly-tablet",
  "version": "1.0.0",
  "private": true,
  "description": "Checklist de seguridad con evidencia por foto y activacion de Shelly",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "mobile:sync": "node scripts/sync-mobile.js",
    "apk:init": "npm run mobile:sync && npx cap add android",
    "apk:sync": "npm run mobile:sync && npx cap sync android",
    "apk:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/camera": "^8.2.0",
    "@capacitor/cli": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
